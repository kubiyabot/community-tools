# Workflow Status

## 🔍 Workflow Details:
{% set r_details = run_details | from_json %}
**Workflow:** {{ workflow_name }} failed at {{ r_details.processed_at }} (UTC). **Status:** {{ r_details.status | title }}, **Conclusion:** {{ r_details.conclusion | title }}. **PR:** [#{{ number }}](https://github.com/{{ repo }}/pull/{{ number }}), **Run:** [View Run](https://github.com/{{ repo }}/actions/runs/{{ r_details.id }}). **Author:** {{ r_details.actor }}.

## 🔬 Detailed Analysis
{% for item in failures_and_fixes %}
### Step: {{ item.step }}
- **Error:** {{ item.error }}
- **Suggested Fix:** 
    - {{ item.fix_description }}
    - **Code Sample:** 
```
{{ item.fix_code_sample }}
```
{% endfor %}


### 📊 Workflow Steps:
```mermaid
graph LR;
{% for step in workflow_steps %}
    {{ loop.index }}["{{ step }}{{ ' ❌' if step in failed_steps else ' ✅' }}"];
    {% if not loop.first %}
    {{ loop.index - 1 }} --> {{ loop.index }};
    {% endif %}
    {% if step in failed_steps %}
    style {{ loop.index }} fill:#ff6b6b,stroke:#333,stroke-width:4px;
    {% else %}
    style {{ loop.index }} fill:#51cf66,stroke:#333,stroke-width:2px;
    {% endif %}
{% endfor %}
```

## 📝 Error Logs
<details>
<summary>Click to expand error logs</summary>

```
{{ error_logs }}
```
</details>
---

#### Was this comment helpful? 👍 or 👎 Please let us know your feedback!

<sub align="center">
🤖 This comment was generated automatically by Kubiya AI at {{ r_details.processed_at }} UTC
</sub>