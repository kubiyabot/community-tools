"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/runtimes/dangerous-in-browser/DangerousInBrowserAdapter.ts
var DangerousInBrowserAdapter_exports = {};
__export(DangerousInBrowserAdapter_exports, {
  DangerousInBrowserAdapter: () => DangerousInBrowserAdapter
});
module.exports = __toCommonJS(DangerousInBrowserAdapter_exports);
var import_toCoreMessages = require("../edge/converters/toCoreMessages.js");
var import_toLanguageModelTools = require("../edge/converters/toLanguageModelTools.js");
var import_runResultStream = require("../edge/streams/runResultStream.js");
var import_toolResultStream = require("../edge/streams/toolResultStream.js");
var import_EdgeChatAdapter = require("../edge/EdgeChatAdapter.js");
var import_createEdgeRuntimeAPI = require("../edge/createEdgeRuntimeAPI.js");
var DangerousInBrowserAdapter = class {
  constructor(options) {
    this.options = options;
  }
  async *run({ messages, abortSignal, config }) {
    const res = await (0, import_createEdgeRuntimeAPI.getEdgeRuntimeStream)({
      options: this.options,
      abortSignal,
      requestData: {
        system: config.system,
        messages: (0, import_toCoreMessages.toCoreMessages)(messages),
        tools: config.tools ? (0, import_toLanguageModelTools.toLanguageModelTools)(config.tools) : [],
        ...config.callSettings,
        ...config.config
      }
    });
    const stream = res.pipeThrough((0, import_toolResultStream.toolResultStream)(config.tools, abortSignal)).pipeThrough((0, import_runResultStream.runResultStream)());
    for await (const update of (0, import_EdgeChatAdapter.asAsyncIterable)(stream)) {
      yield update;
    }
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  DangerousInBrowserAdapter
});
//# sourceMappingURL=DangerousInBrowserAdapter.js.map